/**
 * Re-export shim (Phase 3). `MoveToFolderDialog` moved to `shared/directory`.
 */

export { default } from '../../../shared/directory/MoveToFolderDialog';
export type { MoveToFolderDialogProps } from '../../../shared/directory/MoveToFolderDialog';
