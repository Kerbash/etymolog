/**
 * Re-export shim (Phase 3). `FolderTreeSelect` moved to `shared/directory`.
 */

export { default } from '../../../shared/directory/FolderTreeSelect';
export type { FolderTreeSelectProps } from '../../../shared/directory/FolderTreeSelect';
