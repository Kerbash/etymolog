/**
 * Re-export shim (Phase 3). `FolderNameDialog` moved to `shared/directory`.
 */

export { default } from '../../../shared/directory/FolderNameDialog';
export type { FolderNameDialogProps } from '../../../shared/directory/FolderNameDialog';
